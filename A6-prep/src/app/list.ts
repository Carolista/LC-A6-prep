export class List {

    title: string;
    desc: string;
    items: string[];

    constructor(title: string, desc: string) {
        this.title = title;
        this.desc = desc;
        this.items = []; // always initialized to empty array
    }

    // TODO: write search method?

    // TODO: write sort method?
}
